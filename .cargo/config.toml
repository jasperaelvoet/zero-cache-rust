# The patched workspace `libsqlite3-sys` carries the exact compile options from
# @rocicorp/zero-sqlite3@1.1.2. Do not override them with ambient flags: doing so
# would make the binary's database semantics differ from pinned Zero v1.7.
